{
    "actorOutputSchemaVersion": 1,
    "title": "Domain Vet Audit Report",
    "description": "Comprehensive domain availability and health audit with historical analysis",
    "properties": {
        "report": {
            "type": "string",
            "title": "📊 Visual Audit Dashboard",
            "description": "Interactive HTML report with domain availability status, health score, WHOIS info, SSL/DNS analysis, and historical timeline",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT_REPORT"
        },
        "data": {
            "type": "string",
            "title": "📋 Structured JSON Data",
            "description": "Machine-readable audit data including availability status, scores, and all findings",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}